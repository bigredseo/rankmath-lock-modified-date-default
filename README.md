# rankmath-lock-modified-date-default
Defaults the RankMath "Lock Modified Date" toggle to ON in the WordPress block editor. Fires once per page load and disconnects immediately so users can still manually unlock it per save.
